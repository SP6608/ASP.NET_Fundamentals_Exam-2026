@{
    ViewData["Title"] = "Начало";
}

<div class="container py-5">
    <div class="row align-items-center g-4">
        <div class="col-lg-7">
            <h1 class="display-5 fw-bold mb-3">🚕 Taxi Management System</h1>
            <p class="lead text-muted mb-4">
                Уеб приложение на ASP.NET Core MVC за управление на шофьори, автомобили и таксиметрови услуги.
            </p>

            @if (User.Identity?.IsAuthenticated ?? false)
            {
                <div class="d-flex flex-wrap gap-2">
                    <a class="btn btn-primary" asp-controller="Drivers" asp-action="Details">
                        Шофьори
                    </a>
                    <a class="btn btn-success" asp-controller="Cars" asp-action="Details">
                        Автомобили
                    </a>
                    <a class="btn btn-warning" asp-controller="TaxServices" asp-action="Details">
                        Услуги
                    </a>
                </div>

                <div class="alert alert-info mt-4 mb-0">
                    Влезли сте като <strong>@User.Identity!.Name</strong>.
                </div>
            }
            else
            {
                <div class="d-flex flex-wrap gap-2">
                    <a class="btn btn-primary" asp-area="Identity" asp-page="/Account/Register">
                        Регистрация
                    </a>
                    <a class="btn btn-outline-primary" asp-area="Identity" asp-page="/Account/Login">
                        Вход
                    </a>
                </div>

                <div class="alert alert-warning mt-4 mb-0">
                    За да добавяте коли и услуги, трябва да имате акаунт и да сте влезли в системата.
                </div>
            }
        </div>
